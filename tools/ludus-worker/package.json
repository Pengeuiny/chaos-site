{
  "name": "ludus-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Polls the CHAOS site for sheet rows to interpret and approved jobs to create in Ludus. Runs on a home machine (residential IP) because Ludus sits behind Cloudflare's bot check.",
  "scripts": {
    "start": "node worker.mjs",
    "once": "node worker.mjs --once",
    "interpret": "node interpret.mjs"
  },
  "dependencies": {
    "playwright": "^1.63.0"
  }
}
